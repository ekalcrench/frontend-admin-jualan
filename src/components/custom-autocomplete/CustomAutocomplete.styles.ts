import Box from "@mui/material/Box";
import { styled } from "@mui/material/styles";

export const AutocompleteGroupHeader = styled(Box)(() => ({
  position: "sticky",
  top: "-8px",
  padding: "4px 10px",
  // color: appColors.yankeesBlue,
  fontSize: "12px",
  fontWeight: 600,
  // backgroundColor: appColors.cultured,
}));
