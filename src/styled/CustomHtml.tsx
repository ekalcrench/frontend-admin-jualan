import { styled } from "@mui/material";

export const CustomMain = styled("main")(() => ({
  padding: "16px",
  boxSizing: "border-box",
  flex: "1 1 0",
  minWidth: 0,
  minHeight: 0,
  maxWidth: "100%",
  overflow: "auto",
}));
